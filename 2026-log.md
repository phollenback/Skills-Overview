# 2026 work log — Azure, Azure CLI, and related cloud work

*Last updated: April 1, 2026*

This page summarizes **what can be recovered from this machine and your public GitHub profile** about Azure and `az`-related work. It is not a complete diary: interactive CLI sessions are only visible if they were saved in shell history or written down elsewhere.

---

## How this log was assembled

| Source | What was checked | Result |
|--------|------------------|--------|
| Local `zsh` history | Commands starting with `az` or containing ` az ` | **No matches** — no Azure CLI commands are recorded in the sampled history file. |
| Cursor agent transcripts (local projects) | Text search for Azure / `az` | **No prior Azure-specific threads** beyond routine mentions inside dependencies. |
| Public GitHub (`phollenback`) | Repository names and descriptions | **[Movie-Man](https://github.com/phollenback/Movie-Man)** lists Azure as a deployment target (see below). |
| Local repositories | `~/GitHub`, `~/Cod3` | Several **Azure-themed or Azure-ready** projects (containers, .NET, CI/CD placeholders). |

If you run Azure CLI regularly in another shell, a different profile, or on another machine, that work would not appear here unless you append it manually.

---

## Public GitHub: Movie-Man (explicit Azure deployments)

The **[Movie-Man](https://github.com/phollenback/Movie-Man)** repository description states:

- React movie app (watchlist, **Microsoft Entra ID** auth) used for cloud deployment proof-of-concepts.
- Documented deployment targets include **AWS EC2**, **Azure Container Apps**, and **Azure App Service**.

That is the clearest **on-record** reference to Azure platform work tied to your GitHub account. *(Clone the repo locally if you want to list exact workflows, env vars, or IaC files in a future revision of this log.)*

---

## Local project: Grailtopia-Azure

**Path:** `~/GitHub/Grailtopia-Azure`

- Full-stack **Discogs Helper**–style app: **React** front end (nginx in container), **Node/Express** API, **MySQL 8** with schema init, **Docker Compose** orchestration.
- Suited as a **containerized workload** for cloud hosts (including Azure App Service, Container Apps, or AKS) even though the compose file itself is environment-agnostic.
- API image build supports tests (`SKIP_TESTS` build arg); API service documents **Testcontainers** via Docker socket mount for integration-style testing.

*This folder is a dedicated “Azure” line of the Grailtopia work; push it to GitHub if you want it linked alongside the main [Grailtopia](https://github.com/phollenback/Grailtopia) deployment story.*

---

## Local project: Todo-Blazor (`Cod3/Azure`)

**Path:** `~/Cod3/Azure/Todo-Blazor`

- **Blazor Server** on **.NET 8**, simple todo UI.
- **Multi-stage Dockerfile** (`mcr.microsoft.com/dotnet/sdk` + `aspnet` on Debian bookworm), **`docker compose`** for local port **8080**.
- Fits the same pattern as other **cloud-ready .NET containers** (Azure Container Apps, Web App for Containers, ACI).

---

## Local project: GitHub Actions activity (Azure called out as next step)

**Path:** `~/Cod3/github-actions-activity`

- **Express** API with Docker; **GitHub Actions** builds and pushes to **GHCR**.
- The deploy job is a **placeholder**; comments in the workflow mention **Azure Container Instances** as one possible target alongside Kubernetes, ECS, and Cloud Run.

---

## Related portfolio context (non-Azure-specific)

- **[Grailtopia](https://github.com/phollenback/Grailtopia)** — described on GitHub as production-oriented full-stack app with **AWS EC2** and **GitHub Actions** CI/CD (useful contrast when you want a single narrative that spans AWS and Azure).
- **Skills-Overview [README](README.md)** — homepage for labs, projects, and this log.

---

## Suggested next steps (optional)

1. Run `az account show` and paste **non-secret** metadata (subscription name, tenant id redacted if you prefer) into a dated entry below when you want a true CLI audit trail.
2. After major Azure work, add a **dated bullet** under a new “Running log” section.
3. If **Grailtopia-Azure** or **Todo-Blazor** is published, add repository links in the sections above.

---

### Running log (append here)

- **2026-04-01** — Consolidated recoverable Azure / cloud-adjacent work into this file; fixed homepage link from [README](README.md) to here.
